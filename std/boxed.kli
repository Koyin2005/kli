import std.refs

fun new[T](value : T) -> Box[T] = unsafe
    let ptr = builtins.gc_alloc[T](1);
    builtins.ptr_write(ptr,value);
    builtins.transmute(ptr)
end

fun get[T](this : Box[T]) -> T = this^

fun set[T](this : Box[T], value : T) -> () = this^ = value
