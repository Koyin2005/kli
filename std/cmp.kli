@copy
type Ordering = 
    | Lesser
    | Equal
    | Greater

fun int_cmp(x : Int64, y : Int64) -> Ordering = 
    case x < y of 
    | true -> Ordering.Lesser
    | false -> case x > y of 
        | true -> Ordering.Greater
        | false -> Ordering.Equal
        end
    end
