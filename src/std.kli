fun not(x : bool) -> bool = 
    case x of 
    | true -> false 
    | false -> true 
    end

fun print_string(value : string) -> () = 
    do
        borrow value in r do
            print(value)
        end;
        destroy_string(value)
    end
