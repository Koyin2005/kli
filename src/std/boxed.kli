import std.refs

@opaque
@lang_item("box")
type Box[T] = { }
impl 

    fun alloc(value : T) -> Box[T] = panic

    fun get(value : Box[T]) -> T = panic

    fun set(this : Box[T], value : T) -> () = panic


end