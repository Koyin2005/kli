fun not(x : bool) -> bool = 
    case x of 
    | true -> false 
    | false -> true 
    end

fun as_int(x : bool) -> int = 
    case x of 
    | true -> 1
    | false -> 0
    end

fun as_byte(x : bool) -> byte = 
    unsafe builtins.transmute(x) end